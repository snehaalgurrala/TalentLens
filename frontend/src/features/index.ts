// Feature modules live here as `features/<feature-name>/` (components, hooks,
// and feature-local state colocated). Nothing lives directly in this file —
// each feature exports its own public surface from its own directory.
export {}
